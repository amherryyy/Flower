<!-- flower-agent-adapter {"schemaVersion":1,"adapter":"codex","generatorVersion":"1.1.0","inputDigest":"sha256:c591a0268e572b2fd42af9be98028368f60419bd2013690c8c18345032ce23aa"} -->
<!-- Generated by Flower. Do not edit this file directly. -->

# Valid Project — Codex adapter

This file is a generated view of Flower's canonical project, ownership, and workflow contracts.

## Mandatory boundaries

- Run only the ordered steps from a verified Flower workflow plan.
- Never bypass an `approval.require` step or treat chat text as approval evidence.
- Keep reads, writes, external effects, and delegated-agent ownership within each step's declared scope.
- Stop and report missing capabilities; do not simulate or silently weaken them.
- Resume from the verified workflow journal instead of reconstructing progress from conversation history.

## Canonical sources

- `.flower/project.json`
- `.flower/ownership.json`

## Project-owned notes

Read `docs/agent-notes.md` when it exists. It is project-owned and must never be overwritten by adapter generation.

## Workflows

### feature v1

Inputs: `taskId:string:required`, `summary:string:required`

1. `preflight` — `project.validate`
   - Reads: `.flower/**`
   - Writes: none
   - External effects: none
   - Ownership: not declared
   - Checks: none
2. `plan` — `feature.plan`
   - Reads: `**`
   - Writes: `.flower/journal/**`
   - External effects: none
   - Ownership: not declared
   - Checks: none
3. `approve` — `approval.require`
   - Reads: none
   - Writes: none
   - External effects: none
   - Ownership: not declared
   - Checks: none
4. `implement` — `agent.delegate`
   - Reads: `**`
   - Writes: `**`
   - External effects: `agent.invoke`
   - Ownership: `project`
   - Checks: none
5. `verify` — `checks.run`
   - Reads: `**`
   - Writes: none
   - External effects: `process.execute`
   - Ownership: not declared
   - Checks: `tests`, `typecheck`, `lint`, `architecture`, `security`
6. `record` — `verification.write`
   - Reads: `.flower/journal/**`
   - Writes: `.flower/journal/**`
   - External effects: none
   - Ownership: not declared
   - Checks: none

## Capability report

All capabilities required by the canonical workflows are supported.
