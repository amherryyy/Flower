{
  "$schema": "https://flower.dev/schemas/migration/v1.json",
  "schemaVersion": 1,
  "id": "organizations-001",
  "provider": "postgresql",
  "transaction": "required",
  "destructive": "none",
  "dependsOn": [],
  "verificationQueries": [
    {
      "id": "organizations-fail-closed",
      "sql": "select (select relrowsecurity from pg_class where oid = 'public.organizations'::regclass) and (select relrowsecurity from pg_class where oid = 'public.organization_memberships'::regclass) and not has_table_privilege('public', 'public.organizations', 'select') and not has_table_privilege('public', 'public.organizations', 'insert') and not has_table_privilege('public', 'public.organization_memberships', 'select') and not has_table_privilege('public', 'public.organization_memberships', 'insert') as verified;",
      "expected": true
    }
  ],
  "rollbackGuidance": "Before production use, export dependent project data. Roll back only by dropping organization_memberships before organizations; this destroys tenant data and is not automated.",
  "rls": {
    "tables": ["public.organizations", "public.organization_memberships"],
    "mode": "deny-by-default"
  }
}
