{
  "$schema": "https://flower.dev/schemas/migration/v1.json",
  "schemaVersion": 1,
  "id": "audit-004",
  "provider": "postgresql",
  "transaction": "required",
  "destructive": "none",
  "dependsOn": ["audit-003"],
  "verificationQueries": [{
    "id": "audit-id-generated",
    "sql": "select column_default like '%gen_random_uuid%' as verified from information_schema.columns where table_schema = 'public' and table_name = 'audit_events' and column_name = 'id';",
    "expected": true
  }],
  "rollbackGuidance": "Remove the default from public.audit_events.id. Existing event identifiers remain unchanged.",
  "rls": { "tables": ["public.audit_events"], "mode": "policies-required" }
}
